# <程序名> — 功能规格说明书

> **自动生成** — 由 AI 基于 ABAP 源码分析生成，供业务顾问和开发人员参考。
> 标注 `⚠️ 待确认` 的条目请人工核实。

---

## 1. 程序概要

| 项目 | 内容 |
|---|---|
| 程序名 | `<程序名>` |
| 程序类型 | `<报表/增强/功能模块/接口/转换程序/批导>` |
| 事务码 | `<TCODE 或 N/A>` |
| 业务模块 | `<SD/MM/FI/CO/PP/...>` |
| 一句话描述 | `<一句话说清楚这个程序干什么>` |

---

## 2. 业务场景

- **使用场景**：<什么业务流程中、什么时候会用到这个程序>
- **使用者**：<哪个部门/角色>
- **操作步骤**：
  1. （用事务码或菜单路径进入）
  2. （输入什么条件 / 上载什么文件）
  3. （执行什么操作）
  4. （得到什么结果）
- **执行频率**：<每日/每月/按需>
- **后台/在线**：<后台定时 / 前台在线 / 前台+后台>

---

## 3. 输入 / 输出

### 3.1 选择屏幕

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `<字段名>` | `<输入框/复选框/单选按钮/文件选择>` | `<默认值>` | `<说明>` |

### 3.2 输入文件（如适用）

> 如果有 Excel 上载等文件输入，列出文件结构：
> - **文件名**：<模板名称>
> - **工作表**：<工作表名称 → 对应的 SAP 数据>
> - **数据起始行**：<第几行开始是数据>

### 3.3 输出（ALV 列表 / 报表）

| 字段 | 说明 | 来源 |
|---|---|---|
| `<字段名>` | `<字段说明>` | `<用户输入 / 系统自动 / 从表XX读取 / 计算得出>` |

---

## 4. 涉及数据表

> ⚠️ 只列业务相关的表——模板读取表（如 WWWDATA）、纯技术中间表（如 SMW0 仓储）不列。

### 4.1 读取的表

| 表名 | 描述 | 用途 |
|---|---|---|
| `<TABLE>` | `<描述>` | `<读取什么配置/主数据>` |

### 4.2 写入/更新的表

| 表名 | 描述 | 更新方式 | 用途 |
|---|---|---|---|
| `<TABLE>` | `<描述>` | INSERT / UPDATE / DELETE | `<写入什么数据>` |

---

## 5. 核心业务逻辑

> 用业务语言描述主流程，非代码逐行翻译。

### 主流程

1. （第一步：用户怎么触发、做什么校验）
2. （第二步：从哪些表取什么数据 / 从文件读取什么）
3. （第三步：数据怎么处理、转换、计算）
4. （第四步：输出什么结果 / 写什么表 / 调什么接口）

### 关键计算逻辑

| 业务描述 | 处理方式 | 说明 |
|---|---|---|
| `<描述>` | `<公式/规则/调用方式>` | `<补充说明>` |

---

## 6. 数据流

> 从输入到输出的完整数据路径，用表格表示——确保转 Word 后原生渲染为专业表格。

### 数据流总览

| 阶段 | 步骤 | 数据从哪来 | 做了什么 | 数据到哪去 |
|---|---|---|---|---|
| 输入 | 1 | `<来源>` | `<动作>` | `<去向>` |
| 处理 | 2 | `<上一步>` | `<动作>` | `<下一步>` |
| 输出 | 3 | `<上一步>` | `<动作>` | `<最终目标>` |

### 分步骤详细说明

| 步骤 | 动作 | 数据来源 | 数据去向 | 说明 |
|---|---|---|---|---|
| 1 | `<读/接/校验>` | `<来源>` | `<去向>` | `<做了什么>` |
| 2 | `<处理/计算>` | `<上一步>` | `<下一步>` | `<做了什么>` |
| 3 | `<写/发送/输出>` | `<上一步>` | `<目标>` | `<做了什么>` |

### 多程序/多系统协作（如适用）

| 阶段 | 谁执行 | 做什么 | 传给谁 |
|---|---|---|---|
| 1 | `<程序A>` | `<做什么>` | `<程序B/表/系统>` |
| 2 | `<程序B>` | `<做什么>` | `<最终目标>` |

---

## 7. 外围系统接口

> 如无外部接口，写"无"即可。如有，列出每个接口的详情。

| # | 接口名称 | 方向 | 方式 | 目标系统 | 触发时机 | 传输内容 |
|---|---|---|---|---|---|---|
| 1 | `<接口名>` | 出站/入站 | RFC/HTTP/SOAP/IDoc/文件 | `<目标>` | `<什么时候>` | `<什么数据>` |

### 接口详情

#### 接口 1：<接口名称>

| 项目 | 内容 |
|---|---|
| **调用方式** | `<RFC Destination / HTTP URL / SOAP Endpoint / ...>` |
| **连接配置** | `<SM59 名 / SOAMANAGER 配置 / PI 通道>` |
| **触发条件** | `<什么时候触发>` |
| **请求数据** | `<传出什么数据、什么格式>` |
| **响应数据** | `<接收什么数据>` |
| **异常处理** | `<失败时怎么处理：重试/记录日志/回滚/忽略>` |
| **关联配置表** | `<接口相关的自定义配置表>` |

---

## 8. 关键规则

### 校验规则

| 规则描述 | 不满足时的处理 |
|---|---|
| `<校验条件>` | `<错误提示/跳过/终止/记录日志>` |

### 权限要求

| 权限对象 | 说明 |
|---|---|
| `<权限对象名>` | `<说明>` |
| （如源码无 AUTHORITY-CHECK，写"依赖事务码级别的授权"） | |

---

## 9. ⚠️ 待确认

> 以下信息 AI 无法从源码中确定，需人工补充：

1. <不确定的事项>
2. <不确定的事项>
3. <不确定的事项>

---

*本文件由 AI 自动生成，基于 ABAP 源码静态分析。如有偏差请以实际运行为准。*
